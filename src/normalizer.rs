use solution::PString;

pub fn normalize(strings: Vec<String>) -> Vec<PString> {
    let occ = Vec::with_capacity(strings[0].len());     // Vector de vectores de ocurrencias de caracteres por columna. En otras palabras,
                                                        // occ[i] es un vector con la cuenta de
                                                        // cada caracter ocurrido en la columna i
                                                        // de la matriz representada por `strings`
    for string in strings {

    }
    

    vec![]
}
